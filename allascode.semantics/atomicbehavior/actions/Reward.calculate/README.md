# Reward.calculate

Converts semantic feedback into a bounded learning signal. Explicit rewards are allowed only inside the canonical [-1, 1] range; otherwise the standard AHMP reward mapping is used.
