events:
  Ok: { payload: schema/output.yml }
  Error:
    payload:
      type: object
      required: [code, message]
      properties:
        code: { enum: [NO_CANDIDATES, INVALID_POLICY_STATE, SELECTION_FAILED] }
        message: { type: string }
