# Recovery.generateCandidates

Generates safe recovery alternatives after negative interaction feedback. Recovery is restricted to products already known from the consumer purchase history and always includes `no_action`. A rejection never implies a discount: discount is only one competing candidate.
