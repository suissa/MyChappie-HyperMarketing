# Feedback.record

Validates and projects consumer feedback against the decision that caused the interaction. Feedback is idempotent by `feedbackId` and must correlate to the same consumer and offer.
